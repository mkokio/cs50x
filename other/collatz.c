#include <cs50.h>
#include <stdio.h>

int collatz(int n);

int main(void)
{
    int = get_int("Type a whole number: ")
}

int collatz(int n)
{
    int steps = 0;
    if (n == 1)
    {
        //stop counting
    }
    else if (//even)
    {
        n = n/2;
        steps++
    }
    else
    {
        n = 3 * n + 1;
        steps++
    }
    
}